# Story 4.1: Consistent interpretation across compatible viewers

Status: done

## Acceptance

- Documented that compatible builds share one resolver (`getResolvedTextPaint`) and the same render pipelines; hosts must not strip new JSON keys when persisting scenes.

## Deliverables

- `packages/element/text-paint-serialization.md` — section **Compatible viewers (shared scenes)**.
