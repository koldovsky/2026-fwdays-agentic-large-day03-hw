# OS & editors
.DS_Store
.history/
.idea/
.vscode/

# Secrets & local overrides (never index)
.cursor/mcp.json
.env.local
.env.development.local
.env.production.local
.env.test.local
.envrc

# Dependencies (keep yarn.lock indexable — SSOT for versions when debugging deps)
node_modules/
.yarn/
package-lock.json

# Logs & archives
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tgz

# Build & deploy output
build/
dist/
dev-dist/
.next/
out/
.vercel/

# Test & coverage artifacts
coverage/
.nyc_output/
html/
**/__snapshots__/
**/*.snap

# Generated / bundled / minified (low signal)
*.min.js
*.min.css
meta*.json
packages/excalidraw/types/

# Auto-generated source (from repo scripts / font tooling)
# scripts/buildWasm.js — wasm as base64 TS modules
packages/excalidraw/fonts/wasm/
# scripts/build-locales-coverage.js (yarn locales-coverage)
packages/excalidraw/locales/percentages.json
# cn-font-split output (see "Generated By cn-font-split" in file headers)
packages/excalidraw/fonts/Excalifont/index.ts
packages/excalidraw/fonts/ComicShanns/index.ts
packages/excalidraw/fonts/Xiaolai/index.ts
# Next.js — `next dev` / `next build` (any workspace package)
**/next-env.d.ts

# Repomix / repo pack dumps (huge duplicate of sources; use .repomixignore for packing)
repomix-output.xml

# Tooling caches
.eslintcache
*.tsbuildinfo
vite.config.*.timestamp-*

# Project-specific (mirrors root .gitignore)
firebase/
.claude/

# Git objects
.git/
