# OpenSpec Workflow

This directory contains structured specifications for non-trivial changes.

## Structure

- `specs/` — current baseline system behavior (source of truth)
- `changes/` — proposed or in-progress changes
- `archive/` — completed changes (optional, if used)

---

## Workflow

### 1. Propose

Create a new change under:
